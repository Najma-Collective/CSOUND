# Production deployment configuration
CSOUND_TELEMETRY_ENDPOINT=https://telemetry.csound.example/collect
CSOUND_TELEMETRY_SAMPLE_RATE=1
# NETLIFY_DEPLOY_ALIAS may be left unset so production deploys are immediately live
